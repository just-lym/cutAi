# cutAI 文档正确性与优化报告

生成日期：2026-08-28

## 1. 验证范围

当前工作区 `D:\workspace\cutAI` 只有 Markdown 规格文档和 `.idea` 配置文件，未发现后端、前端、Docker、Alembic、测试等实际项目源码。因此本次验证对象是：

- 12 个 Markdown 文档本身的完整性、内部一致性和可落地性。
- 文档中给出的技术方案、依赖、代码片段、目录结构、API、数据模型、部署方案之间是否互相匹配。
- 是否存在会阻塞代码生成、构建、运行或后续维护的问题。

本次无法完成的验证：

- 无法运行后端/前端构建、单元测试、数据库迁移或 Docker Compose。
- 无法基于实际源码核验函数、模型、API 路由是否真实存在。
- 本地 `npm view` 在线包名核验因网络等待超时被中断；涉及第三方包名和实时 API 价格的项建议在可联网环境中再次确认。

## 2. 总体结论

这些文档更像一组“AI 项目生成提示词/规格书”，不是一个已经实现的项目。作为生成项目的蓝图，整体方向清晰：多 Agent 决策、确定性工具执行、人工审批、版本化时间轴、异步资产处理、前后端分离、单机 Docker 部署。

但当前文档还不能直接作为高可信代码生成输入。主要原因是：

- 文档声明的目录结构与当前仓库完全不匹配，实际没有 `backend/`、`frontend/`、`docker-compose.yml` 等文件。
- 多处同一概念在不同文档中写法不同，例如视频生成默认 Provider、构建后端方式、Chroma collection 命名、B-roll 操作类型。
- 若按文档代码片段直接生成，至少会遇到 Python 导入缺失、未定义变量、TypeScript 语法错误、Docker Compose YAML 无效或依赖包名疑似错误。
- 一些重要字段只在后续文档被使用，未在数据模型中定义，例如 `Asset.processing_step`。
- 部分外部 API、模型名称、价格、Provider 名称需要按官方文档重新核准，尤其是 DashScope、Kling/可灵、Runway/Pika。

建议先修文档再生成代码；如果直接进入实现，需要边生成边重构规格，否则返工概率较高。

## 3. 关键阻塞问题

| 严重度 | 位置 | 问题 | 影响 | 建议 |
|---|---|---|---|---|
| P0 | 当前目录 | 工作区只有 `.md` 文档，没有实际源码 | 无法验证实现正确性，无法构建/测试/运行 | 明确这是“规格仓库”；若目标是项目源码，应先生成或补齐 `backend/`、`frontend/` 等目录 |
| P0 | `01-项目总览.md:112` | `FastAPI(..., lifespan=lifespan)` 中 `lifespan` 未定义 | 直接生成会运行失败 | 增加 lifespan 函数，或移除该参数 |
| P0 | `01-项目总览.md:98` | 使用 `@contextmanager`，但片段未导入 `contextmanager` | 直接生成会 NameError | 在 `database.py` 中补 `from contextlib import contextmanager` |
| P0 | `01-项目总览.md:222` | WebSocket `broadcast` 里使用 `dead.append(ws)`，但 `dead` 未初始化 | 推送异常时会二次异常，连接清理失效 | 在循环前定义 `dead = []`，发送后安全移除 |
| P0 | `01-项目总览.md:188`、`01-项目总览.md:191` | Docker Compose 示例里 `environment: (同 backend)`、`deploy: (同 backend GPU)` 不是合法 YAML | 复制即无法启动 | 必须展开为完整 YAML，或明确标注为伪代码且另给可运行版本 |
| P0 | `06-前端实现.md:108` | `JSON.stringify({ ... })` 是占位符，不是可生成实现 | B-roll select API 客户端无法编译 | 写出真实请求体，例如 `{ candidate_id, insertion, duration_ms }` |
| P0 | `06-前端实现.md:112` | `JSON.stringify({ indices ?? {} })` 不是合法 TypeScript | 前端无法编译 | 改为 `JSON.stringify(indices ?? {})`，并统一参数名 |
| P0 | `02-数据模型.md:192`、`:227`、`:243` | `EditPlan.project_id`、`Job.project_id`、`CloudAPIUsage.project_id` 被写成 `UUID PK` | 会误生成复合/错误主键，破坏一对多关系 | 应为 `UUID FK -> projects.id, indexed` |
| P0 | `10-执行器与流水线.md:73`、`06-前端实现.md:272` | 使用 `asset.processing_step`，但 `02-数据模型.md` 的 Asset 未定义该字段 | 前后端契约断裂 | 在 Asset 模型补 `processing_step: String(64) nullable` |

## 4. 主要一致性问题

| 位置 | 问题 | 建议 |
|---|---|---|
| `01-项目总览.md:59`、`04-工具层与云API.md:177`、`07-使用指南.md:62`、`11-部署与配置.md:14` | 视频生成默认 Provider 一处是 `runway`，多处是 `dashscope` | 统一默认值。建议用 `dashscope` 作为默认，因为它复用同一个 API Key，部署成本最低 |
| `01-项目总览.md:5-38`、`11-部署与配置.md:35-59` | 后端构建系统一处是 `hatchling`，另一处是 `setuptools.build_meta`；包名一处 `aicut`，另一处 `aicut-backend` | 统一为一个 `pyproject.toml`。若是 FastAPI 应用，建议用 `setuptools` 或 `hatchling` 任选其一，并确保 package discovery 可用 |
| `00-后端基础设施..md:95`、`04-工具层与云API.md:125` | `cost_tracker.py` 一处在根目录，一处在 `cloud_api/` | 建议统一到 `app/cloud_api/cost_tracker.py` 或 `app/services/cost_tracker.py` |
| `04-工具层与云API.md:142`、`10-执行器与流水线.md:103` | `embedding_store.py` 一处未指定目录，一处为 `tools/embedding_store.py` | 建议统一到 `app/tools/embedding_store.py` |
| `04-工具层与云API.md:154`、`10-执行器与流水线.md:111` | Chroma collection 命名分别为 `project_{project_id}_assets` 和 `project_{project_id}` | 统一命名。建议 `project_{project_id}_assets`，语义更清楚 |
| `03-多Agent系统.md:169`、`04-工具层与云API.md:73`、`05-API路由层.md:137`、`06-前端实现.md:249` | B-roll 操作类型混用 `INSERT_BROLL` 与 `INSERT_BROLL_OVERLAY` | 统一为 `INSERT_BROLL_OVERLAY`，并在前端 `OP_META` 使用同名 |
| `06-前端实现.md:235` | `FADE_TH` 疑似拼写错误，与后文 `FADE_IN` / `FADE_OUT` 不一致 | 改为 `FADE_IN`、`FADE_OUT` 两个操作 |
| `05-API路由层.md:100`、`09-脚本视图.md:93` | 字幕查询依赖 `Track(type=SUBTITLE) -> SubtitleCue`，但 `SubtitleCue` 绑定的是 Track，不绑定 TimelineVersion 快照 | 如果旧版本时间轴也有字幕，修改当前 cue 可能影响历史版本 | 字幕 cue 应版本化，或将 cue 数据放入 `timeline_json`，或给 `SubtitleCue` 加 `timeline_version_id` |
| `08-快捷键与撤销.md:43-58`、`10-执行器与流水线.md:1-26` | 前端本地 undo 与后端 TimelineVersion restore 的职责边界不够清晰 | 明确“草稿态本地 undo，提交后只能通过版本恢复”，并给 API 流程 |
| `05-API路由层.md:18` | `GET /api/agent/sessions/{session_id}` 描述为“获取会话列表”，路径却是单个 session | 要么改描述为获取单个会话，要么新增列表接口 |
| `10-执行器与流水线.md:136-141` | WebSocket 前端 invalidate 使用空 queryKey | 会刷新不明确，代码生成后效果差 | 写成具体 key：`['timeline', projectId]`、`['subtitles', projectId]`、`['assets', projectId]` |

## 5. 外部依赖/API 风险

| 位置 | 风险 | 建议 |
|---|---|---|
| `06-前端实现.md:15`、`11-部署与配置.md:80` | `@xzarcry/react-timeline-editor` 包名疑似拼写错误。常见写法可能是 `@xzdarcy/react-timeline-editor`，但本地网络未能完成最终核验 | 在联网环境执行 `npm view @xzarcry/react-timeline-editor` 和 `npm view @xzdarcy/react-timeline-editor`，确认后统一 |
| `06-前端实现.md:19` | `react-huna` 未在后文组件中使用，且包名需核验 | 若无明确用途，删除依赖；若用于图标/动画，写明用途 |
| `06-前端实现.md:284-287`、`11-部署与配置.md:84-85` | `react-konva` 在模块 6 依赖列表缺失，但部署文档中补了 | 统一 package.json，确保同时安装 `react-konva` 和 `konva` |
| `00-后端基础设施..md:8`、`:21`、`:33` | ASR 名称出现 `Paraphraser`、`Parafomer`、`Paraformer` 混写 | 统一为官方模型/服务名 `Paraformer` / `paraformer-v2` |
| `00-后端基础设施..md:34`、`04-工具层与云API.md:199-203` | 可灵写成 `Klang`、文件名和环境变量使用 `keling`，API 域名写 `klingai` | 统一命名：用户可见“可灵/Kling”，代码标识建议 `kling`，避免 `keling`/`Klang` 混用 |
| `04-工具层与云API.md:125-139` | LLM/ASR/Embedding 价格可能过期 | 价格表不要硬编码在规格中作为事实来源，建议加 `pricing_updated_at` 和配置覆盖机制 |
| `03-多Agent系统.md:44` | 文档称 LangChain 无官方 DashScope 集成，但这类生态集成变化快 | 生成前应核验是否可用官方/社区 `Tongyi` 或 DashScope 集成，优先减少自研 ChatModel |

## 6. 架构与实现优化建议

### 6.1 先收敛规格，再生成代码

建议新增一份 `SPEC.md` 或把现有文档合并成“唯一事实来源”，至少定义：

- 唯一目录结构。
- 唯一 `pyproject.toml` 与 `package.json`。
- 唯一环境变量表。
- 唯一数据模型字段表。
- 唯一 API OpenAPI 契约。
- 唯一 EditOperation JSON Schema。

这样后续用 AI 生成代码时，不会在不同模块之间继承矛盾。

### 6.2 为 EditOperation 建立强类型 Schema

目前操作类型散落在 Agent、工具、API、前端、Executor 文档中，容易漂移。建议定义 Pydantic discriminated union：

- `DeleteRangeOperation`
- `SetVolumeOperation`
- `InsertBrollOverlayOperation`
- `UpdateSubtitleOperation`
- `FadeInOperation`
- `FadeOutOperation`

前端同步生成 TypeScript 类型，审批、验证、执行都用同一套 schema。

### 6.3 时间轴版本化需要更严格

当前 `TimelineVersion.timeline_json`、`Track/Clip/SubtitleCue` ORM 表、前端本地 undo 同时存在，但边界不清。建议二选一或明确混合策略：

- 简化方案：`TimelineVersion.timeline_json` 是唯一快照，Track/Clip/Cue 只从 JSON 派生展示。
- 关系型方案：每个 Track/Clip/Cue 都归属于一个 `timeline_version_id`，每次提交复制/增量生成新版本。

不建议让 `SubtitleCue` 只挂 Track，却允许跨版本复用，否则历史版本回放和字幕编辑会互相污染。

### 6.4 异步任务与 Job 表对齐

文档有 arq worker、Job 模型、pipeline、WebSocket，但缺少一致的任务状态流。建议：

- 每个 arq task 创建或绑定一个 `Job`。
- Job 中加入 `asset_id`、`step`、`started_at`、`updated_at`。
- pipeline 每步更新 Job 和 Asset。
- WebSocket 广播携带 `job_id`，前端可精确刷新。

### 6.5 文件安全与存储策略

素材导入和上传需要补充安全约束：

- 本地路径导入必须限制允许目录，避免任意读取系统文件。
- 上传文件名要规范化，避免路径穿越。
- 存储应使用内容哈希或 UUID 文件名，原名只作为 metadata。
- 大文件上传建议支持分片或后台处理。
- 删除项目/素材时要有引用检查和清理策略。

### 6.6 云 API 调用建议抽象为 Provider + Budget Guard

文档已有 Provider 概念，但预算控制和 Provider 执行没有完全串起来。建议所有外部调用统一经过：

1. `ensure_budget(...)`
2. provider request
3. `record_usage(...)`
4. retry/backoff
5. structured error mapping

这样费用统计、失败重试、降级策略会更可控。

### 6.7 Docker/GPU 方案需要可运行分层

建议分成三套部署配置：

- `docker-compose.yml`：CPU/通用版，默认可运行。
- `docker-compose.gpu.yml`：NVIDIA GPU overlay。
- `docker-compose.dev.yml`：开发挂载源码和热更新。

这样 macOS/Windows/CI/GPU 机器不需要手工删改 YAML。

### 6.8 前端依赖和 UI 规格应去掉占位内容

模块 6 是最容易直接喂给代码生成器的部分，但里面仍有占位代码和未统一依赖。建议：

- 固定真实 npm 包名和版本。
- 去掉 `JSON.stringify({ ... })` 这类占位。
- 明确 API 请求/响应 DTO。
- 明确各组件 props。
- 避免在“实现文档”中混入演示页视觉文案，保持接口和行为优先。

## 7. 建议修复顺序

1. 把 `00-后端基础设施..md` 文件名改成 `00-项目总览.md` 或 `00-后端基础设施.md`，去掉多余点号，并重新排序文档职责。
2. 统一 Provider 默认值：建议全局使用 `dashscope`。
3. 统一后端构建配置：包名、构建后端、依赖版本只保留一份。
4. 修正所有 P0 代码片段错误：`lifespan`、`contextmanager`、`dead`、YAML 占位、TypeScript 非法语法、错误主键。
5. 补全 Asset 模型字段：`processing_step`，必要时补 `processing_error`、`processed_at`。
6. 建立 `EditOperation` JSON Schema，并在 Agent、API、Executor、前端都引用它。
7. 重新设计 Timeline/Subtitle 的版本化边界。
8. 联网核验第三方包名、外部 API endpoint、模型名和价格。
9. 生成最小可运行后端和前端骨架，再逐模块实现。
10. 增加基础测试：Pydantic schema、timeline executor、SRT parser、API happy path、前端 TypeScript build。

## 8. 可落地性评分

| 维度 | 评分 | 说明 |
|---|---:|---|
| 产品方向清晰度 | 8/10 | 核心场景明确，AI 剪辑平台定位完整 |
| 架构完整度 | 7/10 | 前后端、队列、WS、Provider、预算都有覆盖 |
| 文档一致性 | 5/10 | 多处命名、路径、默认值、操作类型不一致 |
| 代码生成可用性 | 4/10 | 当前片段含占位和运行级错误，需要先清洗 |
| 实际可运行性 | 1/10 | 当前仓库无源码，无法启动 |
| 可优化空间 | 8/10 | 主要是规格收敛、类型约束、版本模型、任务状态链路 |

## 9. 最终建议

当前文档适合作为“需求和架构草稿”，不适合直接当作最终实现说明。建议先做一次规格清洗：把互相矛盾的配置、路径、字段和操作类型统一，再生成代码。最优路线是先落一个最小闭环：

项目创建 -> 上传素材 -> ffprobe 入库 -> 自动生成初始 timeline -> 前端展示 timeline -> Agent 生成 EditPlan -> 人工审批 -> Executor 创建新 TimelineVersion -> WebSocket 通知前端刷新。

这个闭环跑通后，再逐步补 ASR、B-roll 生成、Chroma 检索、费用追踪和 GPU 导出。这样项目会从“宏大的规格”变成“可运行、可测试、可扩展”的工程。
